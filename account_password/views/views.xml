<odoo>
  <data>
    <!-- explicit list view definition -->

    <record model="ir.ui.view" id="account_password_form">
      <field name="name">account_password list</field>
      <field name="model">account_password.account_password</field>
      <field name="arch" type="xml">
        <form>
          <field name="company_id" invisible="True"/>
          <header>
            <button name="action_confirm" type="object" string="Confirm" invisible="state != 'draft'" class="btn-primary"/>
            <button name="action_close" type="object" string="Close" invisible="state != 'confirmed'" class="btn-success"/>
            <button name="action_cancel" type="object" string="Cancel" invisible="state in ('draft','closed','canceled')" class="btn-danger"/>
            <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,closed,canceled"/>
          </header>
          <sheet>
            <div class="oe_title">
            <span class="o_form_label" >Reference code</span>
              <h1 class="d-flex">
                <field name="name" readonly="True" />
              </h1>
            </div>
            <group>
              <group>
                <field name="partner_id" domain="[('company_id', 'in', (False, company_id)), ('supplier_rank', '>', 0)]" readonly="state in ['closed','canceled']"/>
              </group>
              <group>
                <field name="observation" readonly="state in ['closed','canceled']"/>
              </group>
            </group>
            <notebook>
              <page name="invoice_list_p" string="Invoice">
                <!-- <field name="account_move_ids"/> -->
                <field name="account_move_ids" readonly="state in ['closed','canceled']">
                  <tree editable="bottom">
                    <field name="made_sequence_hole" column_invisible="True"/>
                    <field name="name" decoration-bf="1" decoration-danger="made_sequence_hole"/>
                    <field name="partner_id" optional="show"/>
                    <field name="company_id" optional="show"/>
                    <field name="invoice_date" optional="show" column_invisible="context.get('default_move_type') not in ('in_invoice', 'in_refund', 'in_receipt')" readonly="state != 'draft'" string="Bill Date"/>
                    <field name="invoice_date" optional="show" column_invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund', 'out_receipt')" readonly="state != 'draft'" string="Invoice Date"/>
                    <field name="date" optional="hide" string="Accounting Date" readonly="state in ['cancel', 'posted']"/>
                    <field name="invoice_date_due" widget="remaining_days" optional="show" invisible="payment_state in ('paid', 'in_payment', 'reversed') or state == 'cancel'"/>
                    <field name="invoice_origin" optional="hide" string="Source Document"/>
                    <field name="payment_reference" optional="hide" column_invisible="context.get('default_move_type') in ('out_invoice', 'out_refund', 'out_receipt')"/>
                    <field name="ref" optional="hide"/>
                    <field name="invoice_user_id" optional="hide" column_invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund', 'out_receipt')" string="Salesperson" widget="many2one_avatar_user"/>
                    <field name="activity_ids" widget="list_activity" optional="show"/>
                    <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" optional="hide"/>
                    <field name="company_id" groups="!base.group_multi_company" column_invisible="True"/>
                    <field name="amount_untaxed_signed" string="Tax Excluded" sum="Total" optional="show"/>
                    <field name="amount_tax_signed" string="Tax" sum="Total" optional="hide"/>
                    <field name="amount_total_signed" string="Total" sum="Total" decoration-bf="1" optional="show"/>
                    <field name="amount_total_in_currency_signed" string="Total in Currency" optional="show" groups="base.group_multi_currency"/>
                    <field name="amount_total_in_currency_signed" string="Total in Currency" optional="hide" groups="!base.group_multi_currency"/>
                    <field name="amount_residual_signed" string="Amount Due" sum="Amount Due" optional="hide"/>
                    <field name="currency_id" optional="hide" readonly="state in ['cancel', 'posted']"/>
                    <field name="company_currency_id" column_invisible="True"/>
                    <field name="to_check" optional="hide" widget="boolean_toggle"/>
                    <field name="payment_state"
                           string="Payment"
                           widget="badge"
                           decoration-danger="payment_state == 'not_paid'"
                           decoration-warning="payment_state in ('partial', 'in_payment')"
                           decoration-success="payment_state in ('paid', 'reversed')"
                           invisible="payment_state == 'invoicing_legacy' or state != 'posted' or move_type == 'entry'"
                           optional="show"/>
                    <field name="state" widget="badge" decoration-success="state == 'posted'" decoration-info="state == 'draft'" optional="show"/>
                    <field name="move_type" column_invisible="context.get('default_move_type', True)"/>
                  </tree>
                </field>
              </page>
            </notebook>
          </sheet>
        </form>
      </field>
    </record>

    <record model="ir.ui.view" id="account_password_list">
      <field name="name">account_password list</field>
      <field name="model">account_password.account_password</field>
      <field name="arch" type="xml">
        <tree >
          <field name="name"/>
          <field name="partner_id"/>
          <field name="company_id" readonly="1" options="{'no_create': True}"
          groups="base.group_multi_company" optional="hide"/>
          <field name="observation"/>
          <field name="account_move_ids" widget="many2many_tags"/>
          <field name="state" widget="badge" decoration-muted="state == 'draft'" decoration-success="state == 'closed'" decoration-info="state =='confirmed'" decoration-danger="state=='canceled'"/>
        </tree>
      </field>
    </record>

    <record id="account_password_invoice_tree" model="ir.ui.view">
        <field name="name">account.view_invoice_tree</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree/field[@name='name']" position="after">
                <field name="partner_id" column_invisible="context.get('default_move_type') in ('in_invoice', 'in_refund', 'in_receipt', 'out_invoice', 'out_refund', 'out_receipt')"/>
                <field name="password_assigned" column_invisible="True"/>
            </xpath>
        </field>
    </record>


    <!-- actions opening views on models -->

    <record model="ir.actions.act_window" id="account_password_acti_window">
      <field name="name">Invoice password</field>
      <field name="res_model">account_password.account_password</field>
      <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="menu_account_password" name="Invoice password" parent="account.menu_finance_payables" action="account_password_acti_window" sequence="300"/>
  </data>
</odoo>
